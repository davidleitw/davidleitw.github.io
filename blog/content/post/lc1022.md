---
title: "leetcode 1022. Sum of Root To Leaf Binary Numbers [Easy]"
date: 2022-01-11T00:12:53+08:00
draft: false
tags: 
    - leetcode
categories: ["leetcode"]
description: "DFS 遍歷所有根到葉的路徑，以位元左移方式累積二進位值並求和，解析 Sum of Root To Leaf Binary Numbers。"
---

## 題目敘述

![](https://assets.leetcode.com/uploads/2019/04/04/sum-of-root-to-leaf-binary-numbers.png)

> Input: root = [1, 0, 1, 0, 1, 0, 1]
> 
> Output: 22
> 
> Explanation: (100) + (101) + (110) + (111) = 4 + 5 + 6 + 7 = 22

給一棵節點值為 0 或 1 的 binary tree，回傳從 root 到各個 leaf 節點路徑所組成二進位數的總和。

## 解題紀錄

用 DFS 尋訪，每往下一層就把目前的值左移一位再加上當前節點值。到達 leaf（`!root->left && !root->right`）時直接回傳累積的結果。

```c++
class Solution {
public:
    int sumRootToLeaf(TreeNode* root) {
        return solution(root, 0);
    }
    
    int solution(TreeNode *root, int bin) {
        if (!root) return 0;
        bin = (bin << 1) + root->val;
        if (!root->left && !root->right) return bin; 
        return solution(root->left, bin) + solution(root->right, bin);
    }
};
```